DATABASE_URL=postgresql://user:password@localhost:5432/attendance
SECRET_KEY=changeme
DEBUG=false
SESSION_TTL_HOURS=24
# Comma-separated list of trusted frontend origins (no wildcards when credentials are enabled)
CORS_ORIGINS=["http://localhost:5173"]
